comment on column REP_GRID_COLUMNS_CFG."COLUMN_POSITION" is 'the column position within the grid for individual column order';
comment on column REP_GRID_COLUMNS_CFG."COLUMN_VISIBLE" is '''T'' (True) = Visible, ''F'' (False) = not Visible';
comment on column REP_GRID_COLUMNS_CFG."DATATYPE" is '''S'' = String; ''N'' = Number; ''D'' = DateTime, ''B'' = Boolean';
comment on column REP_GRID_COLUMNS_CFG."DEFAULT_VALUE" is 'fill the field with this default value when inserting';
comment on column REP_GRID_COLUMNS_CFG."DEF_VAL_SYS_VAR" is 'use this system variable to set the default value';
comment on column REP_GRID_COLUMNS_CFG."FIXED_MODE" is 'N = not fixed, L = fixed on the left side, R = fixed on the left side';
comment on column REP_GRID_COLUMNS_CFG."GROUPING_LEVEL" is 'level for grouping if grouping is wanted';
comment on column REP_GRID_COLUMNS_CFG."MERGE_CELLS" is 'Merge sorted cells in this column, so the value is only displayed once';



-- sqlcl_snapshot {"hash":"51a007bab7f8b15d53773b1f17f0b81cef3fd659","type":"COMMENT","name":"rep_grid_columns_cfg","schemaName":"dirkspzm32","sxml":""}