comment on column dirkspzm32.rep_grid_columns_cfg.column_position is
    'the column position within the grid for individual column order';

comment on column dirkspzm32.rep_grid_columns_cfg.column_visible is
    '''T'' (True) = Visible, ''F'' (False) = not Visible';

comment on column dirkspzm32.rep_grid_columns_cfg.datatype is
    '''S'' = String; ''N'' = Number; ''D'' = DateTime, ''B'' = Boolean';

comment on column dirkspzm32.rep_grid_columns_cfg.default_value is
    'fill the field with this default value when inserting';

comment on column dirkspzm32.rep_grid_columns_cfg.def_val_sys_var is
    'use this system variable to set the default value';

comment on column dirkspzm32.rep_grid_columns_cfg.fixed_mode is
    'N = not fixed, L = fixed on the left side, R = fixed on the left side';

comment on column dirkspzm32.rep_grid_columns_cfg.grouping_level is
    'level for grouping if grouping is wanted';

comment on column dirkspzm32.rep_grid_columns_cfg.merge_cells is
    'Merge sorted cells in this column, so the value is only displayed once';


-- sqlcl_snapshot {"hash":"ab40a8b6f2b5e8dfd2be247988db5f2b55d667ad","type":"COMMENT","name":"rep_grid_columns_cfg","schemaName":"dirkspzm32","sxml":""}